name: Ingredient Service Docker Image

on:
  workflow_dispatch: { }
  push:
    branches:
      - main
    paths:
      - 'bmsSpring/ingredient-service/**'
      - 'bmsSpring/template-module/**'
      - 'bmsSpring/redis-cache/**'
      - 'bmsSpring/ollama-search/**'
jobs:
  test-template-module:
    uses: ./.github/workflows/test-run.yml
    with:
      app-path: template-module
  test-redis-cache:
    uses: ./.github/workflows/test-run.yml
    with:
      app-path: redis-cache
  test-ollama-search:
    uses: ./.github/workflows/test-run.yml
    with:
      app-path: ollama-search
  test-ingredient-service:
    uses: ./.github/workflows/test-run.yml
    with:
      app-path: ingredient-service
      local-modules: ollama-search,redis-cache,template-module
  build-and-push-ingredient-service:
    needs: [ test-template-module, test-redis-cache, test-ollama-search, test-ingredient-service ]
    uses: ./.github/workflows/build-and-push.yml
    with:
      image-name: ingredient-service-bms
      context: ./bmsSpring
      dockerfile: ./bmsSpring/ingredient-service/Dockerfile
    secrets:
      DOCKERHUB_PASSWORD: ${{ secrets.DOCKERHUB_PASSWORD }}
      DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}